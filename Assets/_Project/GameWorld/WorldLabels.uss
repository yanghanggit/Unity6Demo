/* ===================================================================
   世界坐标跟随文本标签：独立 PanelRenderer（Sort Order 比 HUD 低，渲染在其下方），
   全屏容器本身不需要拦截任何点击（这个 Panel 里没有任何可交互元素）。
   每个 .world-label 由 WorldLabelPanelController 按世界坐标换算出的屏幕位置
   实时设置 left/top，并用 translate 把锚点对齐到文本底部中心，使文本悬浮在目标正上方。
   =================================================================== */
.world-labels-root {
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0;
    left: 0;
}

.world-label {
    position: absolute;
    translate: -50% -100%;
    color: white;
    font-size: 24px;
    -unity-text-align: middle-center;
    -unity-font-style: bold;
    white-space: nowrap;
}
